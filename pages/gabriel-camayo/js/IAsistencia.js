class IAsistencia {

    registrarEntrada(fechaHora) {
        // Método que todas las clases deberían tener
    }

    registrarSalida(fechaHora) {
        // Método que todas las clases deberían tener
    }
}

export default IAsistencia;